# 集团仓库管理系统：项目状态与发布交接

> 文档生成：2026-08-22（Asia/Shanghai）
> 线上发布、共享前门与企业微信校验文件核验截至：2026-08-22
> 完整时间线与原始证据：[PROJECT_STATUS.md](../PROJECT_STATUS.md)

## 当前结论

- 手机响应式、入库批次自动生成和出库空态对齐已完成本地/手机验收，并已发布到服务器。
- 当前服务器发布版本为 `20260822T023810Z-3f61393e00633d7efac09918b7e60fe4b06acd2f-3531208`，对应源码提交 `3f61393e00633d7efac09918b7e60fe4b06acd2f`。
- 2026-08-21 已将 `codex/production-deployment@b2bedbf` 无冲突合并回 `feat/warehouse-system`，本地集成提交为 `a03e87e`；本次合并未重新部署。
- 共享 Caddy 前门的固定资产站点隔离（`77619dc`）和企业微信校验文本提交 `d41db296` 均已随本次授权发布部署：`assets.beikexiang.cn` 仅反代 `beikexiang-assets:8088`，仓库域名及 API 路由保持独立；固定资产项目源码未修改。
- 企业微信人工客服已处理此前的域名主体校验。仓库校验文本已上线，服务器经生产主机名确认返回精确 16 字节 `uIghWtRwsuUPacAx`；企业微信后台已启用可信域名 `warehouse.beikexiang.cn` 并配置可信 IP `106.14.224.213`。经用户授权，当前账号已加入应用内部管理员映射；旧会话角色固化修复已随 `3f61393` 发布，服务器与本机公网核验均通过，待在企业微信客户端刷新应用确认后台权限。
- 当前长对话的开发与交接工作已经收口，后续功能将在新任务中继续；新任务应以本文和 `PROJECT_STATUS.md` 为上下文入口，不依赖旧对话全文。
- 本轮没有 Push，也没有修改生产 Secret、删除生产数据卷或触碰无关项目。
- 服务器内部健康核验通过；2026-08-21 同机公网复验也已确认仓库与固定资产双域名的受信 HTTPS 健康端点均为 200。企业微信真实业务全链路和生产主数据尚未完成验收，因此仍不能标记为“正式生产验收完成”。

## 已交付范围

### 电脑端

电脑端仍是完整操作端，保留库存台账、主数据、期初库存、入库、审批出库、调拨、退库、盘点、月结、报表和导出等能力。

### 手机端

手机端为现有 React 网页的响应式适配，不新增独立 App，已支持：

- 库存、物品、仓库和批次查询；
- 通知与待办查看；
- 单页登记入库；
- 四步实际出库与取消操作。

以下复杂操作在手机端明确提示“请在电脑端处理”，不挂载可写表单：物品库、仓库维护、期初库存、调拨、退库、盘点、月结。

### 本轮关键规则与界面收敛

- 正常登记入库时，选择采购日期后由服务端统一生成批次号，格式为 `YYYYMMDD-001`（例如 `20260814-001`）；手机端仅展示只读预览，不能手工填写。期初库存与历史批次仍保留原有手工批次规则。
- 手机端采购日期、批次预览和采购人使用同规格的 44px 输入控件，日期文字左对齐；标题按一级 25px、二级 18px、三级 15px 递减。
- 首页仓库选择与快捷操作使用一致的 14px 字号并保留至少 44px 触控高度；出库空态图标与文字已成组对齐。

## 生产发布与核验

### 已完成的服务器核验

- 2026-08-22 经用户授权发布 `20260822T023810Z-3f61393e00633d7efac09918b7e60fe4b06acd2f-3531208`。上传归档 SHA-256 `31b6006c612b334860ab7366cba9e264ef1b495a81c5e9917585735099c997b7` 与本机一致；发布脚本创建 PostgreSQL 备份、构建 migrate/API/Web、执行迁移/seed 并启动服务。API、PostgreSQL、Web 均为 `healthy`，仓库健康端点报告 Prisma 与数据库 `ok`，企业微信校验文本精确匹配，固定资产入口仍为 HTTP 200；未删除数据卷、未修改生产 Secret、未 Push、未触碰 `yuemuhui`。
- 2026-08-22 经用户授权发布 `20260821T165246Z-d41db29673c2e169654b89741bf35ae948939ab5-2896536`。上传归档的服务器 SHA-256 与本机一致；发布脚本执行发布前 PostgreSQL 备份、镜像构建、迁移/seed 和服务启动，未删除数据卷或触碰无关项目。
- 原授权发布完成时 `warehouse-prod` 中 API、PostgreSQL、Web 均为 `healthy`，镜像均标记为当前发布版本。其后管理员映射热更新曾仅强制重建 API；本次 `3f61393` 授权发布已替代该临时 `warehouse/api:latest` 标签，当前 API、Web 和 migrate 均使用显式发布标签。
- 服务器通过生产主机名验证仓库首页 HTTP 200、企业微信校验文本精确为 `uIghWtRwsuUPacAx`、固定资产首页 HTTP 200。该结果同时验证共享前门的双域名隔离没有破坏固定资产入口。

### 外部入口边界

历史上本机对公网 HTTPS 健康端点的请求曾在发布前后均被连接重置，不能覆盖或否定服务器内部健康核验。2026-08-21 后续同机只读复验显示：两个域名都解析到 `106.14.224.213`，80/443 TCP 连通；仓库 HTTP 健康端点由 Caddy 308 跳转至 HTTPS，仓库与固定资产 HTTPS 健康端点均以受信证书返回 200，固定资产首页亦返回 200。两个站点的 `Permissions-Policy` 分别为 `camera=()` 与 `camera=(self)`，提供了按主机名隔离的外部行为证据。此前本机连接重置已排除；手机公网复验尚未执行，下一次授权发布前仍须从服务器内部复核容器、共享网络和 Caddy 运行时配置。

## 企业微信接入状态

企业微信登录、审批详情、审批回调及补同步的代码接缝已存在，但**手机端尚未完成企业微信真实接入验收**。

当前企业微信接入状态：固定资产可信域名由用户报告已配置；仓库可信域名 `warehouse.beikexiang.cn` 已启用，企业可信 IP `106.14.224.213` 已配置。人工客服处理主体校验后，仓库曾因线上路径返回 SPA HTML 而无法通过所有权校验；现已发布校验文本，服务器复验为精确 16 字节 `uIghWtRwsuUPacAx`。企业微信客户端的“暂无后台权限”已确认属于应用内部角色映射；经用户明确授权，已将当前唯一申请人账号加入生产管理员映射并重建 API。旧会话会复用 Cookie 中的角色；新发布版本改为在读取有效会话时按其中的 `weComUserId` 实时重算角色，仍待当前客户端刷新仓库应用确认。

后续按以下顺序完成配置与验收：

1. 刷新仓库应用，确认当前账号可以进入管理员后台。企业微信应用的可见范围、应用负责人和应用内部角色不是同一配置。
2. 以真实接口调用确认 `60020` 不再出现。
3. 在授权边界内确认回调参数和审批模板，配置审批回调地址并重启服务，再用测试审批完成“审批通过 → 待出库 → 实际出库 → 报表”的真实全链路验收，确认不会重复建单。

本文件不记录任何企业微信 Secret、回调 Token、加密密钥或管理员凭据。

## 待办与风险

| 优先级 | 待办 | 完成标准 |
| --- | --- | --- |
| P0 | 完成 HTTPS 入口的手机公网复验 | 当前电脑已留下 DNS、TCP、受信 TLS 和双域名健康检查证据；还需在独立手机网络稳定打开两个域名并留存结果。 |
| P0 | 确认已发布的会话角色实时解析修复在企业微信中生效，并完成后台角色、回调与全链路验收 | 校验文本、可信域名、可信 IP、当前账号管理员映射与新版本服务器核验均已完成；既有会话刷新后的角色、回调、模板映射和一张测试审批均核验通过。 |
| P0 | 确认三个仓库正式名称并导入期初库存 | 管理员与财务核对数量、金额及导入结果。 |
| P1 | 核验自动备份和一次恢复演练 | 确认每日任务实际运行，并完成可恢复性验证。 |
| P1 | 统一重复批次的服务端业务错误 | 将数据库唯一性冲突稳定映射为可定位的批次字段错误。 |
| P2 | 复验 Docker 部署测试的默认并行稳定性 | 在 Docker 空闲时重跑默认完整 Vitest；仅当超时重复出现时再评估测试调度或超时策略。 |

## 操作边界

- 当前本地集成基线为 `feat/warehouse-system@3f61393`（包含历史合并 `a03e87e` 与共享前门提交 `77619dc`）；生产部署历史仍保留在 `codex/production-deployment`。下一次发布必须使用包含该提交的代码，并先完成完整门禁，不能用未同步的旧分支覆盖服务器。
- 生产 PostgreSQL 使用 Docker 持久卷；升级和排障不得删除数据卷。
- 服务器上的其他项目不在本系统操作范围内，部署或清理时不得触碰。
- 任何新发布都应遵循：本地测试 → 数据库备份 → 镜像发布 → 健康检查 → 保留可回滚版本，并在真实企业微信链路和生产主数据完成前避免宣称正式上线。
- 如需推送远程仓库、重新部署或修改企业微信/生产配置，必须另行获得授权。

## 证据与详细记录

2026-08-22 授权发布前的定向企业微信校验文件测试为 2/2 通过，API/Web 类型检查、生产构建和 `git diff --check` 均通过。服务器收到的发布归档 SHA-256 与本机一致；部署日志确认备份、构建、迁移/seed 与服务启动完成。发布后的实时核验以本文“已完成的服务器核验”和 [PROJECT_STATUS.md](../PROJECT_STATUS.md) 的 10.13 为准。

同日企业微信自建应用页面已显示仓库可信域名为已启用、企业可信 IP 为 1 个；企业微信客户端的“暂无后台权限”来自应用内部角色映射，而不是可信域名/IP。随后用户明确要求将当前登录账号设为管理员：已从生产数据中识别唯一申请人账号，更新生产运行配置的管理员白名单并强制重建 API。运行时配置、API `healthy` 状态、仓库首页 HTTP 200 和校验文本均已复核；本文不记录该账号的 `userid`，未修改生产业务数据、Secret、回调 Token、EncodingAESKey、审批模板或应用可见范围。随后关闭/重开应用的截图仍为领用人，根因是角色写入最长 8 小时的加密会话 Cookie，前端在 Cookie 有效时直接采用 `/auth/session` 的旧角色。经用户授权，本地以 RED→GREEN 新增集成测试并实现每次读取企业微信会话时按 `weComUserId` 实时解析白名单角色；本地绕过会话带有经签名的 `isLocalAuth` 标记，但仅在当前本地绕过开关启用时保持其测试角色。认证测试为 19/19 通过（企业微信 4、本地绕过 13、会话服务 2），完整测试为 48 个文件通过、3 个跳过（262/262 通过、17 个跳过），API/Web 类型检查、生产构建和 `git diff --check` 均通过，认证安全复审无阻塞项。该修复已发布为 `20260822T023810Z-3f61393e00633d7efac09918b7e60fe4b06acd2f-3531208`；服务器与本机公网健康、校验文本和固定资产隔离均通过，待当前账号刷新企业微信应用确认管理员界面。

已核验的发布前门禁：Node 24 下非部署 Vitest 为 46 个文件通过、242 个测试通过、17 个跳过；隔离内存模式 E2E 为 121/121 通过；API/Web 类型检查与 Web 构建通过。

本次交接文档变更后又执行了新鲜本机门禁：`corepack pnpm test` 为 48 个文件通过、3 个文件跳过；256 个测试通过、17 个测试跳过、0 个失败（25.98s）。本次未重新部署，服务器状态仍以本文件前述 2026-08-14 核验及 [PROJECT_STATUS.md](../PROJECT_STATUS.md) 为准。

分支整合后的新鲜门禁同样为 48 个文件通过、3 个文件跳过，256 个测试通过、17 个测试跳过；重新生成 Prisma Client 后，API/Web 类型检查与生产构建均通过。本次只完成本地合并与文档同步，没有 Push 或重新部署。

共享前门的本地新鲜门禁：先新增固定资产覆盖文件断言并观察到 11 项中 1 项按预期失败（缺少 `deploy/frontdoor-assets.override.yml`），补齐最小 `web` 镜像覆盖后部署配置 Vitest 为 11/11 通过；Docker Engine 27.2.0 下，以示例环境和临时 `FRONTDOOR_IMAGE` 运行 `docker compose ... config --quiet` 通过，未启动或构建容器。`corepack pnpm typecheck`、`corepack pnpm build`（Web 1613 modules transformed）和 `git diff --check HEAD~1..HEAD` 均通过。变更提交为 `77619dc`；当时线上仍是 `20260814T093837Z-fada1d6738d6-8012`，授权发布前的核验要求已在随后 2026-08-22 的实际发布中完成。

分支收口时，默认文件并行的完整 Vitest 曾一次在既有 Linux 打包验证中出现本机 `spawnSync docker ETIMEDOUT`；同一单文件随后为 4/4 通过，Docker Engine 27.2.0 与缓存的 `node:24-alpine` 镜像均正常，且最近提交没有改动该测试。关闭文件并行后完整套件为 48 个文件通过、3 个文件跳过，258 个测试通过、17 个跳过、0 个失败（43.86s）。这记录为本机 Docker 调度时序风险，不代表共享前门的产品回归；后续仅在默认并行超时重复时再调整测试策略。

该次只读公网与控制台复验没有发布、重启、修改安全组、执行服务器 Shell 命令、操作数据库/Secret 或企业微信。阿里云控制台显示 ECS 实例 `i-uf6ig2xdl67rqerk67l1` 运行中、健康状态正常，公网 IP 仍为 `106.14.224.213`。这是一项发布前外部入口证据；随后已在 2026-08-22 完成包含 `77619dc` 的实际发布。

企业微信仓库域名排障的本地门禁：先新增仓库 Web public 校验文本测试并观察到缺文件的预期 RED，新增最小静态文本后该测试为 2/2 通过；`corepack pnpm typecheck`、`corepack pnpm build` 通过，生产构建产物确认含 16 字节校验文本。该段是发布前事实；随后已发布且核验文本内容正确，企业微信后台设置仍未保存。

完整的 Git 基线、测试命令、服务器发布与回滚记录、临时 SSH 清理记录及后续状态更新，均以 [PROJECT_STATUS.md](../PROJECT_STATUS.md) 为唯一完整事实来源。

## 新任务启动清单

新任务开始后先执行以下检查，再讨论或修改代码：

1. 完整阅读 `D:\桌面\仓库\PROJECT_STATUS.md` 和本文件。
2. 在 `D:\桌面\仓库` 运行 `git status --short --branch`、`git log -3 --oneline --decorate` 和 `git stash list`。
3. 确认当前集成分支为 `feat/warehouse-system`，不要假定存在 `main`；已核验的历史合并提交为 `a03e87e`，共享前门代码提交为 `77619dc`、其 Docker 时序交接提交为 `77e722b`，实际起点以当前 `HEAD` 为准。
4. 不自动恢复或删除 `stash@{0}`，不清理或提交工作区原有的未跟踪计划文档、`.tmp_ppt/` 和演示资料。
5. 先明确本次新需求和验收标准，再选择最小可行开发流程；涉及新功能、认证、数据模型或部署时使用独立分支或 worktree。
6. 本地代码状态与服务器状态严格区分：当前服务器运行 `20260822T023810Z-3f61393e00633d7efac09918b7e60fe4b06acd2f-3531208`；任何后续 Push、发布、生产数据库或企业微信配置操作都需重新取得授权。

可直接在新任务中使用以下开场指令：

> 继续开发“集团仓库管理系统”。先完整阅读 `D:\桌面\仓库\PROJECT_STATUS.md` 和 `D:\桌面\仓库\docs\项目状态与发布交接.md`，再检查 Git、stash 和工作区状态。不要假定存在 `main`，不要自动恢复或删除 `stash@{0}`，保留原有未跟踪文件。当前服务器版本与本地代码不同，未经授权不要 Push、部署或操作生产数据。完成前更新两份状态文档。

### 紧凑桌面布局（本地集成，2026-08-22）

- 本地 `codex/compact-desktop-layout` 已加入获批调整：`821px–1180px` 默认 `64px` 图标侧栏，悬停和键盘焦点临时以 `232px` 覆盖且工作区保持 `64px`；指针固定时工作区留边为 `232px`，再次点击收起，刷新默认收起；`<=820px` 的手机导航未变。`980px/1180px` 紧凑顶栏、指标与仪表盘行为亦包含在内。
- 自动化侧栏 E2E 新鲜结果为 7/7 通过，覆盖指针固定/取消固定、键盘取消固定、指针到键盘焦点、刷新及 `1181px`/`820px` 边界。`corepack pnpm typecheck`、`corepack pnpm build` 和 `git diff --check` 均以 exit 0 完成。
- `corepack pnpm test` 未通过：唯一失败为 `tests/deployment/deployment-scripts.test.ts` 在 Docker bind-mount 此 Windows worktree 时的 `spawnSync docker ETIMEDOUT`；47 个文件和 261 个测试通过，3 个文件和 17 个测试跳过。该项为已诊断但未解决的本地环境验证阻塞，不是产品测试通过的结论。
- 本轮未 Push、未部署、未修改生产数据库、Secret 或企业微信配置。服务器仍运行既有 `3f61393` 发布版本；任何线上发布仍需重新取得明确授权。
