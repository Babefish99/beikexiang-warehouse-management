# 集团仓库管理系统：项目状态与发布交接

> 文档更新：2026-09-02（Asia/Shanghai）
> 生产发布、企业微信回调、审批补同步与自动备份核验截至：2026-09-02
> 完整时间线与原始证据：[PROJECT_STATUS.md](../PROJECT_STATUS.md)

## 当前结论

- 当前生产源码为 `ae4bd80ef66445d9da0c44e0dfd2fc1b1ec5465f`，服务器发布版本记录为 `20260901T090016Z-ae4bd80ef664-3056372`。2026-09-02 已从服务器当前源码重建 API 镜像并重建容器；精确 Git `HEAD` 以实时状态为准。
- GitHub Actions CI-only PR #13 和生产收口 PR #14 均已合并；`ef1b8cf` 是 PR #14 的合并提交。
- 企业微信审批 `202609010007` 已手工重同步成功，审批状态为 `APPROVED`，仓库状态为 `PENDING_OUTBOUND`。物品 `ACCEPT-61AD5B0-01` 的单位已更正为“件”；数据库复核为 1 条审批明细、`OUTBOUND_COUNT=0`、`LEDGER_COUNT=0`，因此未发生实际出库或库存流水。
- 企业微信自建应用已保存生产回调 URL 并启用审批状态通知事件，平台 URL 握手成功。生产 API 已切换为严格 `NODE_ENV=production`，加载回调 Token、43 字符 EncodingAESKey 和 40 字符目标模板 ID；本文不记录实际敏感值。
- 审批 `202609020008` 已同步为 `APPROVED/PENDING_OUTBOUND`，1 条明细为 `1.0000 件`。生产签名加密 POST 回调返回 200 `success`，重复同步成功；数据库实际出库单为 0，没有扣减库存。
- `warehouse-backup.service` 与 `warehouse-backup.timer` 已安装并启用。服务器时区为 `Asia/Shanghai`，timer 为每天 02:30，附加 0–10 分钟随机延迟；当前 active，下一次计划为 2026-09-02 02:30 后。
- 手工触发的计划备份 `warehouse-20260901T092006Z-3074723.sql.gz` 为 8306 字节，manifest 为 264 字节，均为 `root:root`、权限 `600`；gzip 和 SHA-256 校验通过。该备份已恢复到无网络、无生产卷、无开放端口的临时 PostgreSQL 容器，验证 26 张 public 基础表、1 条物料、1 条审批、0 条出库单和 2 条历史库存流水；临时容器已清理，生产 Web/API/PostgreSQL 仍为 healthy。
- 当前期初工作簿可修复结构，但仍缺少 223 个实盘数量；`WH-01 + BJ0008` 缺确认单价，`WP0010` 分类、两组重复名称和盘点基准日期需要人工确认。生产尚未导入期初库存。
- 系统仍不能标记为“正式生产验收完成”：企业微信回调的代码与配置阻塞已关闭，但期初数据尚未完成，实际出库与报表验收也未执行；未来执行实际出库必须取得单独授权。

## 已交付范围

### 电脑端

电脑端仍是完整操作端，保留库存台账、主数据、期初库存、入库、审批出库、调拨、退库、盘点、月结、报表和导出等能力。

### 手机端

手机端为现有 React 网页的响应式适配，不新增独立 App，已支持：

- 库存、物品、仓库和批次查询；
- 通知与待办查看；
- 单页登记入库；
- 四步实际出库与取消操作。

以下复杂操作在手机端明确提示“请在电脑端处理”，不挂载可写表单：物品库、仓库维护、期初库存、调拨、退库、盘点、月结。

### 本轮关键规则与界面收敛

- 正常登记入库时，选择采购日期后由服务端统一生成批次号，格式为 `YYYYMMDD-001`（例如 `20260814-001`）；手机端仅展示只读预览，不能手工填写。期初库存与历史批次仍保留原有手工批次规则。
- Prisma `ProcurementBatch` 唯一约束冲突在本地新实现中统一为 HTTP 409 和稳定消息 `batch number already exists`；自动批次仍最多重试 3 次，其他模型的唯一约束错误不会误报为批次重复。前端同时保留弹窗和表单草稿，在批次字段显示中文错误及 `aria-invalid`。
- 手机端采购日期、批次预览和采购人使用同规格的 44px 输入控件，日期文字左对齐；标题按一级 25px、二级 18px、三级 15px 递减。
- 首页仓库选择与快捷操作使用一致的 14px 字号并保留至少 44px 触控高度；出库空态图标与文字已成组对齐。
- 聚焦式 P1 修正统一了桌面/手机出库、调拨和退库状态的中文展示，手机“服务端 ID”改为“业务编号”，物品页技术字段改为“企业微信选项标识”；成功/错误反馈分别补充 `role="status"`/`role="alert"`，期初库存失败重试按钮使用既有次要按钮样式。该批次只做低风险收敛，不改变服务端状态、API、权限或数据库行为。

### 期初库存 Excel 导入

- 仅电脑端管理员可使用；单次只接受一个不超过 5 MB 的 `.xlsx`，先预览校验，再以绑定管理员、文件摘要和有效期的短时凭证提交同一文件。
- 固定验证 81 条物品和 243 条三仓盘点行；零库存行完整验证后跳过写库，缺失物品与正库存批次、余额、明细、流水和导入摘要在同一原子事务中写入。
- `填写说明!B6` 是盘点基准日期，服务端据此生成批次并检查会计期间；管理员必须记录线下共同核对的财务复核人。工作簿公式缓存、金额和校验状态均不作为服务端业务真相。
- 旧单行 `POST /admin/opening-stock` 已退役并返回 HTTP 410；正式导入成功后只能通过盘点调整纠错，不允许重导、覆盖或直接删除。
- 功能收尾阶段的本地 PostgreSQL 16 验证覆盖 Prisma 主数据、业务存储、期初导入事务和重启持久化，共 24/24 通过；带数据库真正串行的完整 Vitest 为 58 个文件、365/365 通过。合入集成分支后，默认完整 Vitest 为 54 个文件通过、4 个文件条件跳过，341 个测试通过、24 个跳过；隔离期初导入/移动边界 E2E 为 37/37，API/Web 类型检查和生产构建通过。
- 当前工作簿审计确认：81 条物品资料存在，但固定 243 个仓库/物品组合中只有 204 个，39 个组合缺失；223 个数量为空，`WH-01 + BJ0008` 的正库存缺确认单价，`WP0010` 分类以及 `CY0017/CY0018`、`WP0001/WP0010` 两组重复名称需人工确认，盘点基准日期也需业务确认。生产尚未导入，阻塞数据不得推断或代填。

## 生产发布与核验

### 已完成的服务器核验

- 2026-09-01 已发布 `20260901T090016Z-ae4bd80ef664-3056372`，对应源码 `ae4bd80ef66445d9da0c44e0dfd2fc1b1ec5465f`。该版本包含每日自动备份的 systemd service/timer；生产健康检查通过，未执行实际出库或期初库存导入。
- 2026-09-02 生产 API 切换为严格 `NODE_ENV=production`，回调配置已加载；从服务器当前源码重建 `warehouse/api:latest` 后，运行镜像已确认包含企业微信 `apply_data.contents` 规范化逻辑。API 容器与公网健康接口均正常。
- 自动备份 timer 已启用且 active，服务器时区 `Asia/Shanghai`，每天 02:30 后随机延迟 0–10 分钟执行。手工触发验证生成 `warehouse-20260901T092006Z-3074723.sql.gz`（8306 字节）及 manifest（264 字节），两者均为 `root:root 600`，gzip 与 SHA-256 校验通过；隔离恢复演练已通过且临时容器已清理。
- 2026-08-22 经用户授权发布 `20260822T023810Z-3f61393e00633d7efac09918b7e60fe4b06acd2f-3531208`。上传归档 SHA-256 `31b6006c612b334860ab7366cba9e264ef1b495a81c5e9917585735099c997b7` 与本机一致；发布脚本创建 PostgreSQL 备份、构建 migrate/API/Web、执行迁移/seed 并启动服务。API、PostgreSQL、Web 均为 `healthy`，仓库健康端点报告 Prisma 与数据库 `ok`，企业微信校验文本精确匹配，固定资产入口仍为 HTTP 200；未删除数据卷、未修改生产 Secret、未 Push、未触碰 `yuemuhui`。
- 2026-08-22 经用户授权发布 `20260821T165246Z-d41db29673c2e169654b89741bf35ae948939ab5-2896536`。上传归档的服务器 SHA-256 与本机一致；发布脚本执行发布前 PostgreSQL 备份、镜像构建、迁移/seed 和服务启动，未删除数据卷或触碰无关项目。
- 2026-08-22 的 `3f61393` 发布曾替代管理员映射热更新使用的临时 `warehouse/api:latest` 标签；这是旧版本历史，当前以 `ae4bd80` 发布为准。
- 服务器通过生产主机名验证仓库首页 HTTP 200、企业微信校验文本精确为 `uIghWtRwsuUPacAx`、固定资产首页 HTTP 200。该结果同时验证共享前门的双域名隔离没有破坏固定资产入口。

### 外部入口边界

历史上本机对公网 HTTPS 健康端点的请求曾在发布前后均被连接重置，不能覆盖或否定服务器内部健康核验。2026-08-21 后续同机只读复验显示：两个域名都解析到 `106.14.224.213`，80/443 TCP 连通；仓库 HTTP 健康端点由 Caddy 308 跳转至 HTTPS，仓库与固定资产 HTTPS 健康端点均以受信证书返回 200，固定资产首页亦返回 200。两个站点的 `Permissions-Policy` 分别为 `camera=()` 与 `camera=(self)`，提供了按主机名隔离的外部行为证据。此前本机连接重置已排除；手机公网复验尚未执行，下一次授权发布前仍须从服务器内部复核容器、共享网络和 Caddy 运行时配置。

## 企业微信接入状态

企业微信登录、审批详情、审批回调及补同步的代码接缝已存在；回调配置、平台保存握手、生产验签解密和幂等同步均已验证。

当前企业微信接入状态：仓库可信域名 `warehouse.beikexiang.cn` 已启用，企业可信 IP `106.14.224.213` 已配置，校验文本已上线。自建应用的回调 URL 已保存并启用审批状态通知事件。审批 `202609020008` 已同步为 `APPROVED/PENDING_OUTBOUND`，1 条明细为 `1.0000 件`；首次失败来自旧运行镜像，重建含 `normalizeDetail` 的当前源码镜像后补同步成功，正式签名加密的公网回调再次成功且没有生成实际出库单。

后续按以下顺序完成剩余验收：

1. 下一张自然发生的目标模板审批到达时，只读确认平台来源 POST 的运行记录；不再为回调代码或配置创建阻塞项。
2. 人工确认并复核期初库存工作簿后执行生产导入。
3. 实际出库和报表验收留待未来单独授权；当前不得为验收而扣减库存。

本文件不记录任何企业微信 Secret、回调 Token、加密密钥或管理员凭据。

## 待办与风险

| 优先级 | 待办 | 完成标准 |
| --- | --- | --- |
| 已完成 | 企业微信回调与审批同步 | 回调保存握手、签名加密 POST、真实审批详情解析、`PENDING_OUTBOUND` 与重复同步幂等均通过；实际出库单为 0。 |
| P0 | 修复、复核并导入三仓期初库存 | 人工确认 223 个数量、39 个缺失组合、`WH-01 + BJ0008` 单价、`WP0010` 分类、重复名称和盘点日期后，重新预览并由财务复核；生产当前未导入。 |
| 已完成 | 隔离恢复演练 | 备份已恢复到隔离临时 PostgreSQL 容器并完成结构/关键计数核验；临时容器计数为 0，生产服务保持 healthy。 |
| P1 | 完成实际出库与报表验收 | 当前审批停在待出库且库存未变；实际扣库及报表验收必须未来单独授权。 |

2026-08-24 已关闭此前的 Docker 部署测试环境门禁：动态测试目录的精确父目录获 Docker Desktop 文件共享授权后，定向部署测试 4/4、默认完整 Vitest 262/262 均通过，无需调整测试并行或超时策略。

## 操作边界

- 当前生产源码为 `ae4bd80ef66445d9da0c44e0dfd2fc1b1ec5465f`；收口分支在其上仅增加测试稳定性与交接文档。GitHub Actions CI-only PR #13 已合并。后续整合仍须实时读取 Git 状态并保证只做快进安全更新。
- 生产 PostgreSQL 使用 Docker 持久卷；升级和排障不得删除数据卷。
- 服务器上的其他项目不在本系统操作范围内，部署或清理时不得触碰。
- 任何新发布都应遵循：本地测试 → 数据库备份 → 镜像发布 → 健康检查 → 保留可回滚版本，并在真实企业微信链路和生产主数据完成前避免宣称正式上线。
- 用户已授权在当前收口范围内自主推进开发和生产配置；但企业微信通知订阅、隔离恢复环境清理以及实际出库等受即时确认或单独授权约束的操作仍须遵守对应边界。

## 证据与详细记录

2026-09-02 企业微信回调收口证据：自建应用已保存生产回调 URL 并启用审批状态通知事件；生产 API 以严格 `NODE_ENV=production` 加载回调配置，运行镜像包含 `apply_data.contents` 规范化逻辑。审批 `202609020008` 补同步后为 `APPROVED/PENDING_OUTBOUND`，1 条明细为 `1.0000 件`；正式签名加密公网 POST 返回 200 `success`，第 2、3 次同步均成功，实际出库单为 0。仍未完成的是期初数据人工确认/导入，以及需要未来单独授权的实际出库与报表验收。

2026-09-01 生产收口证据：`ae4bd80ef66445d9da0c44e0dfd2fc1b1ec5465f` 已发布为 `20260901T090016Z-ae4bd80ef664-3056372`；审批 `202609010007` 手工重同步后为 `APPROVED/PENDING_OUTBOUND`，`ACCEPT-61AD5B0-01` 单位为“件”，出库单和库存流水计数均为 0。systemd 自动备份 timer 已启用并 active，时区为 `Asia/Shanghai`，手工触发生成的备份及 manifest 权限、gzip、SHA-256 和隔离恢复均通过；临时恢复容器已清理，生产服务保持 healthy。

2026-08-25 全局搜索与仓库范围 P1 经只读代码审计和现有自动化复验关闭。库存查询服务与路由 Vitest 为 2 个文件、5/5 通过；隔离 API `3421`、Web `5381`、memory/local-auth 下，`workspace-tools.spec.ts` 与 `dashboard.spec.ts` 为 10/10。证据覆盖 `warehouseId=all` 和指定单仓、搜索结果中的仓库/批次/数量/金额、跳转库存查询、范围切换后的旧请求隔离、筛选持久化、首页入库/出库指标刷新。没有新增产品或测试代码；未 Push、部署或操作生产环境，保留 stash、未跟踪资料和其他 worktree。

2026-08-25 聚焦式 P1 分支从根集成起点 `70d7764` 创建；在确认根分支是 `codex/ui-consistency-audit@2adf2c8` 祖先后，使用 `git merge --ff-only` 无冲突快进到本地 `feat/warehouse-system@2adf2c8`。合入后默认完整 Vitest 为 55 个文件通过、4 个条件跳过，354 个测试通过、25 个跳过；相关 6 个 Playwright spec 在隔离端口及 memory/local-auth 下为 40/40；API/Web 类型检查、生产构建（Web 1615 modules transformed）和 `git diff --check 70d7764..HEAD` 均通过。没有 Push、PR、部署或生产环境操作，`stash@{0}`、未跟踪资料和其他 worktree 均未改动。

2026-08-25 聚焦式 P1 界面一致性修正按 TDD 在隔离工作树完成。RED 先确认统一状态映射模块缺失，并在隔离仓库服务上确认英文枚举、旧技术字段、缺失 alert/status 语义、字号差异和无变体重试按钮；GREEN 后状态映射单元测试 13/13、Web 单元测试 60/60、受影响的 6 个 Playwright spec 40/40 通过。默认完整 Vitest 为 55 个文件通过、4 个条件跳过，354 个测试通过、25 个跳过；API/Web 类型检查、生产构建（Web 1615 modules transformed）和 `git diff --check` 均通过。E2E 使用隔离 API `3413`、Web `5376`、memory/local-auth，未复用固定资产服务。本批次没有全量 CSS 重构、后端历史英文错误清理或宽弹窗迁移；没有 Push、部署、生产数据库、Secret 或企业微信配置操作，根工作区 stash、未跟踪资料和其他 worktree 均未改动。

2026-08-25 在重复批次修复本地合入后执行完整隔离 Playwright。首轮为 138/139，唯一失败是旧主数据 spec 仍操作已由 Excel 导入替代且返回 410 的单行期初库存表单；现行期初导入专用 spec 已覆盖错误预览、提交冲突、成功摘要和禁止重复初始化。删除这一条过期测试后，相关两个 spec 为 10/10，完整 E2E 为 138/138。该门禁覆盖管理员/财务角色、入库、审批出库、调拨、退库、盘点、月结、报表与 Excel 导出；它只关闭本地自动化证据缺口，不等同于企业微信真实回调、正式数据或生产验收。本轮没有产品代码、Push、部署或生产配置变更。

2026-08-25 重复批次冲突统一按 TDD 在隔离工作树完成。RED 分别确认前端字段错误为空、移动端批次预览没有 `aria-invalid`、修改采购日期后错误未清除，以及真实 PostgreSQL 的手工重复、自动分配重试耗尽和期初库存第二行冲突均泄漏原始 Prisma `P2002`。GREEN 后服务端只识别 `ProcurementBatch` 的相关唯一约束并返回稳定 HTTP 409；`InboundBatchSequence` 并发冲突保留自动重试，其他模型的 `P2002` 不会误标。新鲜验证为：入库表单单元测试 14/14、Prisma 业务存储 14/14、入库 E2E 11/11、完整 Vitest 58 个文件且 366/366、API/Web 类型检查、生产构建（Web 1614 modules transformed）全部通过。首次完整数据库套件因一次性临时库 `public` schema 未应用迁移导致旧主数据 3 项失败；仅对无持久卷本地测试容器应用仓库 5 个迁移后，该套件 3/3 和完整复跑均通过。临时 PostgreSQL 容器已删除，测试端口已释放；提交 `9eadaa1` 已仅快进合入本地 `feat/warehouse-system`，本项没有 Push、部署、生产数据库、Secret 或企业微信配置操作。

2026-08-24 期初库存 Excel 导入的本地实现覆盖管理员专属 5 MB `.xlsx` 预览与一次性提交、81/243 固定契约、零库存验证后跳过、缺失物品和正库存原子写入、盘点基准日期/财务复核审计以及旧端点 410。收尾评审补齐预览“总数量”、拒绝无表头额外业务列，并消除问题列表重复 React key。2026-08-25 功能分支门禁为针对性 Vitest 99/99、默认完整 Vitest 341 个通过/24 个条件跳过、API/Web 类型检查与生产构建通过、隔离 E2E 37/37、`git diff --check` 通过。随后使用无持久卷且已删除的本地 PostgreSQL 16 容器补跑 4 个 Prisma 条件套件；修正旧主数据测试漏列规范分类 `HJ` 的两处断言后，主数据、业务存储、期初导入事务和重启持久化共 24/24 通过，包含原子提交与故障回滚。带 PostgreSQL 的默认文件并行完整套件为 364/365，仅旧历史 seed 迁移测试受固定 5 秒并发超时影响；该测试单独通过，真正串行的完整套件为 58 个文件、365/365 通过（46.46s），不放宽超时。临时容器和端口已清理。实际远端分支已在本轮开始前存在；本轮按用户选择只把 `700eb74` 本地快进集成到 `feat/warehouse-system`，没有新的 Push、部署、生产数据库、Secret 或企业微信配置操作。合入后默认完整 Vitest 再次为 341/341 个实际执行测试通过、24 个条件跳过，隔离 E2E 为 37/37；根工作区重新生成 Prisma Client 后，API/Web 类型检查与生产构建通过，Web 转换 1614 个模块。

2026-08-22 授权发布前的定向企业微信校验文件测试为 2/2 通过，API/Web 类型检查、生产构建和 `git diff --check` 均通过。服务器收到的发布归档 SHA-256 与本机一致；部署日志确认备份、构建、迁移/seed 与服务启动完成。发布后的实时核验以本文“已完成的服务器核验”和 [PROJECT_STATUS.md](../PROJECT_STATUS.md) 的 10.13 为准。

同日企业微信自建应用页面已显示仓库可信域名为已启用、企业可信 IP 为 1 个；企业微信客户端的“暂无后台权限”来自应用内部角色映射，而不是可信域名/IP。随后用户明确要求将当前登录账号设为管理员：已从生产数据中识别唯一申请人账号，更新生产运行配置的管理员白名单并强制重建 API。运行时配置、API `healthy` 状态、仓库首页 HTTP 200 和校验文本均已复核；本文不记录该账号的 `userid`，未修改生产业务数据、Secret、回调 Token、EncodingAESKey、审批模板或应用可见范围。随后关闭/重开应用的截图仍为领用人，根因是角色写入最长 8 小时的加密会话 Cookie，前端在 Cookie 有效时直接采用 `/auth/session` 的旧角色。经用户授权，本地以 RED→GREEN 新增集成测试并实现每次读取企业微信会话时按 `weComUserId` 实时解析白名单角色；本地绕过会话带有经签名的 `isLocalAuth` 标记，但仅在当前本地绕过开关启用时保持其测试角色。认证测试为 19/19 通过（企业微信 4、本地绕过 13、会话服务 2），完整测试为 48 个文件通过、3 个跳过（262/262 通过、17 个跳过），API/Web 类型检查、生产构建和 `git diff --check` 均通过，认证安全复审无阻塞项。该修复已发布为 `20260822T023810Z-3f61393e00633d7efac09918b7e60fe4b06acd2f-3531208`；服务器与本机公网健康、校验文本和固定资产隔离均通过，待当前账号刷新企业微信应用确认管理员界面。

已核验的发布前门禁：Node 24 下非部署 Vitest 为 46 个文件通过、242 个测试通过、17 个跳过；隔离内存模式 E2E 为 121/121 通过；API/Web 类型检查与 Web 构建通过。

本次交接文档变更后又执行了新鲜本机门禁：`corepack pnpm test` 为 48 个文件通过、3 个文件跳过；256 个测试通过、17 个测试跳过、0 个失败（25.98s）。本次未重新部署，服务器状态仍以本文件前述 2026-08-14 核验及 [PROJECT_STATUS.md](../PROJECT_STATUS.md) 为准。

分支整合后的新鲜门禁同样为 48 个文件通过、3 个文件跳过，256 个测试通过、17 个测试跳过；重新生成 Prisma Client 后，API/Web 类型检查与生产构建均通过。本次只完成本地合并与文档同步，没有 Push 或重新部署。

共享前门的本地新鲜门禁：先新增固定资产覆盖文件断言并观察到 11 项中 1 项按预期失败（缺少 `deploy/frontdoor-assets.override.yml`），补齐最小 `web` 镜像覆盖后部署配置 Vitest 为 11/11 通过；Docker Engine 27.2.0 下，以示例环境和临时 `FRONTDOOR_IMAGE` 运行 `docker compose ... config --quiet` 通过，未启动或构建容器。`corepack pnpm typecheck`、`corepack pnpm build`（Web 1613 modules transformed）和 `git diff --check HEAD~1..HEAD` 均通过。变更提交为 `77619dc`；当时线上仍是 `20260814T093837Z-fada1d6738d6-8012`，授权发布前的核验要求已在随后 2026-08-22 的实际发布中完成。

分支收口时，默认文件并行的完整 Vitest 曾一次在既有 Linux 打包验证中出现本机 `spawnSync docker ETIMEDOUT`；同一单文件随后为 4/4 通过，Docker Engine 27.2.0 与缓存的 `node:24-alpine` 镜像均正常，且最近提交没有改动该测试。关闭文件并行后完整套件为 48 个文件通过、3 个文件跳过，258 个测试通过、17 个跳过、0 个失败（43.86s）。这记录为本机 Docker 调度时序风险，不代表共享前门的产品回归；后续仅在默认并行超时重复时再调整测试策略。

该次只读公网与控制台复验没有发布、重启、修改安全组、执行服务器 Shell 命令、操作数据库/Secret 或企业微信。阿里云控制台显示 ECS 实例 `i-uf6ig2xdl67rqerk67l1` 运行中、健康状态正常，公网 IP 仍为 `106.14.224.213`。这是一项发布前外部入口证据；随后已在 2026-08-22 完成包含 `77619dc` 的实际发布。

企业微信仓库域名排障的本地门禁：先新增仓库 Web public 校验文本测试并观察到缺文件的预期 RED，新增最小静态文本后该测试为 2/2 通过；`corepack pnpm typecheck`、`corepack pnpm build` 通过，生产构建产物确认含 16 字节校验文本。该段是发布前事实；随后已发布且核验文本内容正确，企业微信后台设置仍未保存。

完整的 Git 基线、测试命令、服务器发布与回滚记录、临时 SSH 清理记录及后续状态更新，均以 [PROJECT_STATUS.md](../PROJECT_STATUS.md) 为唯一完整事实来源。

## 新任务启动清单

新任务开始后先执行以下检查，再讨论或修改代码：

1. 完整阅读 `D:\桌面\仓库\PROJECT_STATUS.md` 和本文件。
2. 在 `D:\桌面\仓库` 运行 `git status --short --branch`、`git log -3 --oneline --decorate` 和 `git stash list`。
3. 不要假定存在 `main`；旧集成基线为 `feat/warehouse-system@6abeef7`，当前生产源码为 `ae4bd80ef66445d9da0c44e0dfd2fc1b1ec5465f`，实际起点仍以实时 `HEAD` 为准。
4. 不自动恢复或删除 `stash@{0}`，不清理或提交工作区原有的未跟踪计划文档、`.tmp_ppt/` 和演示资料。
5. 先明确本次新需求和验收标准，再选择最小可行开发流程；涉及新功能、认证、数据模型或部署时使用独立分支或 worktree。
6. 本地代码状态与服务器状态严格区分：当前服务器运行 `20260901T090016Z-ae4bd80ef664-3056372`；自动回调订阅和隔离恢复演练仍需即时确认，实际出库需未来单独授权。

可直接在新任务中使用以下开场指令：

> 继续开发“集团仓库管理系统”。先完整阅读 `D:\桌面\仓库\PROJECT_STATUS.md` 和 `D:\桌面\仓库\docs\项目状态与发布交接.md`，再检查 Git、stash 和工作区状态。不要假定存在 `main`，不要自动恢复或删除 `stash@{0}`，保留原有未跟踪文件。先核对当前 `HEAD` 与生产版本 `20260901T090016Z-ae4bd80ef664-3056372`，遵守回调订阅、恢复演练和实际出库的授权边界。完成前更新两份状态文档。

### 紧凑桌面布局（本地集成，2026-08-22，2026-08-24 复验）

- 来源分支 `codex/compact-desktop-layout@46b1b22` 的获批调整已通过合并提交 `7b85a76` 进入 `feat/warehouse-system`：`821px–1180px` 默认 `64px` 图标侧栏，悬停和键盘焦点临时以 `232px` 覆盖且工作区保持 `64px`；指针固定时工作区留边为 `232px`，再次点击收起，刷新默认收起；`<=820px` 的手机导航未变。`821px/980px` 紧凑顶栏保留当前页、角色及头像/菜单控件且无水平溢出；`1180px` 网格、指标与仪表盘仍可读，`1181px`/`820px` 保持既有桌面/手机边界。
- 2026-08-24 自动化侧栏 E2E 新鲜结果为 14/14 通过（29.0s），以仓库专属临时端口和进程级隔离配置（API `127.0.0.1:13001`、Web `127.0.0.1:13000`、memory persistence、local-auth）运行。默认端口会复用本机固定资产系统，曾使失败快照显示错误项目；隔离端口验证的是当前仓库工作树，测试结束后端口与进程均已释放，未停止或修改固定资产系统。除 ADMIN/FINANCE 既有标签与 href、悬停/固定/取消固定、焦点路径、工作区收起、刷新和既有桌面/手机边界外，覆盖 `980px` 展开品牌边界、`821px`/`980px`/`1080px` 焦点顺序几何、`821px`/`980px`/`1180px` 长姓名控件收容，以及切换控件的可访问名称/`aria-expanded`、导航文字提示和完整 Tab 路径至当前用户菜单。`corepack pnpm typecheck`、`corepack pnpm build` 和文档修改后的 `git diff --check` 均以 exit 0 完成；构建转换 1613 个 Web 模块。
- 隔离本地浏览器视觉验收（不构成生产或企业微信验收）：`820px` 显示手机底部导航与手机入库表单、无桌面侧栏或页面溢出；`980px` 显示 64px 紧凑侧栏、保留顶栏上下文/角色、两列指标及单列仪表盘，入库表单可读无溢出，悬停为 232px 覆盖/64px 工作区，固定为 232px 覆盖/232px 工作区；`1180px` 为紧凑侧栏、四项指标和单列仪表盘，入库表单可读且无水平溢出；`1440px` 为完整 232px 侧栏与双列仪表盘，入库表单亦可读且无水平溢出。适配修正后 `980px` 展开品牌实测 Logo 190px、切换控件 32px、间距 3px，均在品牌范围内。
- Docker Desktop 本地门禁已解除：用户手动加入并应用精确父目录 `D:\桌面\仓库\.worktrees\compact-desktop-layout\.superpowers` 后，只读 bind-mount 探针返回 `bind-ok`，定向部署测试 4/4 通过（26.01s）。此前 `spawnSync docker ETIMEDOUT` 的根因是动态 `release-test-*` 目录的父目录未获文件共享授权，不需要修改产品代码、测试并行或超时策略。
- 新鲜完整 Vitest 以当前仓库 API `127.0.0.1:13001` 和完整健康地址 `http://127.0.0.1:13001/health` 运行，48 个文件通过、3 个跳过；262 个测试通过、17 个跳过、0 个失败（20.12s）。一次并行验证曾使固定 5 秒的启动契约测试超时，该测试单独运行 1/1 通过（59ms）；这是本机资源竞争证据，不是产品回归。
- 2026-08-24 已按用户选择把 `codex/compact-desktop-layout@46b1b22` 本地合并到 `feat/warehouse-system`。冲突处理以获批紧凑交互和 14 项 E2E 为主，同时保留集成分支新增的登录测试稳定性处理及切换按钮 hover/focus 可见样式，移除旧的并行状态机与对应单元测试。合并暂存结果再次通过侧栏 E2E 14/14（28.1s）、完整 Vitest 48 个文件通过/3 个跳过且 262 个测试通过/17 个跳过（13.75s）、API/Web 类型检查、生产构建和 `git diff --check`。
- 合并提交 `7b85a76` 的提交后复验再次通过：侧栏 E2E 14/14（26.0s）；完整 Vitest 48 个文件通过、3 个跳过，262 个测试通过、17 个跳过、0 个失败（36.26s）；API/Web 类型检查、生产构建和最终 `git diff --check` 均通过。本次拥有的 `.worktrees/compact-desktop-layout` worktree 与已合并来源分支随后已清理，其他 worktree 未动。
- 该历史轮次只完成本地合并与验证，当时没有生产变更，服务器版本为 `3f61393`；当前生产版本见文档顶部。

### 三仓期初库存 Excel 模板（本地交付，2026-08-24）

- 三个正式仓库已按系统现有编码确定：`WH-01` 集团二楼仓库、`WH-02` 内区1号仓库、`WH-03` 1区车库后仓库。
- 已只读参考 `D:\桌面\酒水2026年7月.xlsx` 的“基础资料”和“汇总”，在 `outputs/warehouse-initial-import-template/集团仓库期初数据导入模板.xlsx` 生成独立五表模板；未覆盖或重命名源 Excel，输出工作簿不加入 Git。
- 模板生成完成时的历史指纹为 58,239 字节、SHA-256 `7673B6C689C061E8A4544B493A98754FDA03F66BDE4D46CBB59C9C83366E8D08`。2026-08-24 收尾阶段只读复核原工作区同名文件时，其大小已变为 75,532 字节且存在 Excel 临时锁文件；检查时的 `物品资料` 表头不再符合固定 8 列导入契约。文件可能仍在人工编辑，本轮没有覆盖或修复；正式导入前必须关闭/完成编辑，并重新通过系统预览校验。
- 模板包含 `填写说明`、`仓库清单`、`物品资料`、`期初库存`、`核对汇总`，数据规模为 3 个仓库、81 条物品、243 条盘点行和 81 条核对行。黄色单元格由人工填写，灰色为引用/公式，红色为阻塞错误，绿色为通过；空白数量表示未盘点，`0` 表示已盘点且库存为零。
- 结构、行数、唯一编码、关键公式和公式错误扫描均通过；`BJ0016` 的内存联动场景验证了日期驱动批次号、三仓数量/金额汇总、差异和状态自动更新。五张工作表均已完成视觉渲染检查；API/Web 类型检查、生产构建（Web 转换 1613 个模块）和 `git diff --check` 均通过。默认并行完整 Vitest 一次仅在既有 `bootstrap.test.ts` 的 5 秒健康请求上超时；健康端点实时为 200、该单测独立 1/1 通过（68ms），禁用文件并行后的完整套件为 48 个文件通过、3 个跳过，262 个测试通过、17 个跳过、0 失败（49.50s），无需修改产品或测试代码。
- 源数据异常已保留并提示：`CY0008` 原综合期末库存 `-1`，`BJ0009`/`BJ0034`/`BJ0051` 原汇总缺失，`WP0010` 类别疑似错位；任何异常都未复制为实盘数量。
- 仍待人工完成盘点基准日期、三仓实盘数量/确认单价及管理员与财务复核；系统 Excel 预览/整批事务导入已进入本地 `feat/warehouse-system@700eb74`，但尚未 Push 或部署，任何数据库均未导入。本轮未修改生产数据库、Secret 或企业微信配置；线上版本仍为 `3f61393` 发布版本。

### GitHub Actions CI（已合并，2026-08-31）

- GitHub Issue #3 是当前唯一 CI 规格，标签为 `ready-for-agent`。本地 `codex/github-actions-ci` 从已与远端同步的 `feat/warehouse-system@bc9d6fd4caaa10cfb3a1562eec422c34134d56cf` 创建；旧 `ddafb7b` 未合并、未复用、未删除。
- 新增 `quality` 与 `e2e` 两个独立检查，固定 `ubuntu-24.04`、Node `24.19.0`、pnpm `11.20.0`、PostgreSQL 16、官方 Action 完整 SHA、只读权限、pnpm store 缓存、Docker 预拉取、并发取消和 30/45 分钟超时。`quality` 执行 Prisma 生成/迁移、完整串行 Vitest、根 typecheck/build 和事件提交范围 diff-check；`e2e` 执行完整 Chromium 套件并在失败时保留 7 天的报告、trace 和 screenshot 产物。
- YAML 契约测试按 RED→GREEN 完成。收尾门禁为契约 4/4、PostgreSQL 迁移 5/5、完整串行 Vitest 60 文件且 383/383、完整 Chromium E2E 140/140、API/Web typecheck、生产 build（Web 1615 modules transformed）和 `git diff --check` 全部通过。过程中修复了新工作树缺少显式 Prisma Client 生成、额外 `--` 导致 Vitest 串行参数失效/测试状态污染，以及 preview token 测试篡改 base64url 尾字符可能仍解码为同一签名字节的随机失败。
- CI-only PR #13 已完成远端验证并合并，`6abeef7` 为合并后的旧集成基线。首轮远端 `quality` 发现通知单测依赖 Windows/Ubuntu ICU 对同优先级中文标题的排序差异，后改为验证集合完整与优先级序列；产品逻辑未变。
- 远端 `e2e` 已完整通过；`quality` 还发现旧启动测试误用 `localhost:3001` 外部进程，现通过当前 `buildServer()` 的 Fastify 注入自包含验证 `/health` 并关闭实例。本地定向 1/1 与完整串行 383/383 均通过。分支保护是否启用须以 GitHub 实时状态为准。
